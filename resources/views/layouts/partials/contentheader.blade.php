<!-- Content Header (Page header) -->
<section class="content-header">
    <h1>
        @yield('contentheader-title')
        <small>@yield('contentheader-description')</small>
    </h1>
    @yield('breadcrumb-html')   
</section>